# Optional. The canonical site address used in metadata, the sitemap and robots.txt.
# On Vercel it falls back to the project's production domain, locally to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=
